import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Chronova — Premium Wall Calendar",
  description:
    "A beautifully crafted interactive wall calendar with date range selection, notes, and dark mode.",
  keywords: ["calendar", "date picker", "wall calendar", "chronova"],
  openGraph: {
    title: "Chronova — Premium Wall Calendar",
    description: "Interactive wall calendar with date range selection and notes.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
